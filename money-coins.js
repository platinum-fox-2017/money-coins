function moneyCoins(num) {
    let result = []
    var exchangeRate = [10000, 5000, 2000, 1000, 500, 200, 100, 50, 20, 10, 1]
    if (num <= 0) {
        return result
    } else {
        for (let [index, value] of arabicNumeral.entries()) {
            console.log(index, value)
        }
    }
}